部署：
	1.将bus/lib（或者\target\ibmp-bus-0.0.2-SNAPSHOT\lib）下的所有jar放到部署的lib中

	2.将\target\classes下的配置文件放到部署的classes中

	3.将..\target下的function.xml和function_*.xml放到部署的bus启动文件同目录

更新：

	代码更新，只需要将\target下的ibmp-bus-0.0.2-SNAPSHOT.jar覆盖到部署的lib中即可


	配置文件更新，将\target\classes下的配置文件放到部署的classes中



config.ini文件配置：
#------------------------------配置开始----------------------------------------------
[Java]

#JAVA虚拟机参数(只识别如下三个参数，其它参数无效)
VMOptions=-Xmx512m -Xms512m -Xss128k

#接收启动和停止事件的实现类
EventClass=com.thinkive.event.EventSink

#是否显示GC信息(0:不显示 1:显示 缺省为0)
GCTrace=0

[Server]

;服务器窗口标题
WindowTitle=云柜台TBAS_2_test

#服务器名称
ServerName=BusinessBus

;服务器监听端口(缺省为10000)
ListenPort=12212

;允许的最大连接数
MaxConnNum=2000

;最大工作线程数
WorkThreadNum=30

;等待队列最大长度
MaxQueueLength=65536

;允许连接本服务器的客户机IP地址列表，以|分隔。若为空，则不限制来源IP(SourceIPList=127.0.0.1|10.100.104.218|10.100.104.212)
SourceIPList=

#客户超时时间(单位:秒,缺省1分钟)
ClientTimeout=60

;日志记录的级别,不区分大小写(ALL<DEBUG<INFO<WARN<ERROR<FATAL<OFF),缺省记录全部
LogLevel=INFO

#是否需要登录(0:不需要 1:需要 缺省为0)
NeedLogin=0

#任务执行时间报警值(单位:秒)(超过此时间会记录报警日志,缺省为10秒)
TaskTimeWarn=10

;是否对响应数据加密(0:不加密  1:加密  缺省为0)
EncEnable=0

;数据通信密钥(用于请求数据的解密和响应数据的加密)
Key=216E0D565E294060

;功能模块XML列表(多个文件以"|"分隔)
ModuleXMLList=function.xml|function_cyb.xml|function_fxckh.xml|function_ggt.xml|function_zk.xml|function_rzrq.xml
